// The order of the entries is important:
// More specific entries (like `en-US`) should come first, followed by more generic entries (like `en`).
// See https://github.com/formatjs/formatjs/issues/4469 for more information.
export const allLocales = ['en-US', 'de-DE'];

export const defaultLocale = allLocales[0];
